<header>
  <div class="header-inner">
    <a href="/" class="logo">Hana Code Corp</a>
    <nav>
      <a href="#about">About</a>
      <a href="#apps">Apps</a>
      <a href="#contact">Contact</a>
    </nav>
  </div>
</header>

<style>
  header {
    border-bottom: 3px solid var(--color-border);
    background: var(--color-card-bg);
  }

  .header-inner {
    max-width: 800px;
    margin: 0 auto;
    padding: 1rem 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    font-weight: 900;
    font-size: 1.25rem;
    text-decoration: none;
    color: var(--color-text);
  }

  nav {
    display: flex;
    gap: 1.5rem;
  }

  nav a {
    text-decoration: none;
    color: var(--color-text);
    font-weight: 700;
    text-transform: uppercase;
    font-size: 0.85rem;
    letter-spacing: 0.05em;
  }

  nav a:hover {
    color: var(--color-accent);
  }

  @media (max-width: 768px) {
    .header-inner {
      padding: 1rem;
    }

    nav {
      gap: 1rem;
    }

    nav a {
      font-size: 0.75rem;
    }
  }
</style>
